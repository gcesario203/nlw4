<style>
    .container{
        width: 800px;
        justify-content: center;
        align-items: center;
        align-content: center;
        display: flex;
        flex-direction: column;
    }

    .value{
        padding: 10px;
        margin: 5px;
        background: #8257e6;
        color: #fff;
        border-radius: 6px;
        width: 15px;
        text-align: center;
        text-decoration: none;
    }

    .level{
        display: flex;
        margin: 10px;
        justify-content: space-between;
        width: 350px;
    }

    .answers{
        width: 350px;
        display: flex;
        justify-content: space-between;
    }
</style>
<div class="container">
    <label>Olá, <strong>{{name}}</strong>! Tudo bem?</label>
    <h3>{{title}}</h3>
    <br/>
    <strong>{{description}}</strong>

    <div class="level">
        <span>Pouco provável</span>
        <span>Muito proável</span>
    </div>

    <div class="answers">
        <a class="value" href="{{link}}/1?u={{userId}}&s={{surveyId}}">1</a>
        <a class="value" href="{{link}}/2?u={{userId}}&s={{surveyId}}">2</a>
        <a class="value" href="{{link}}/3?u={{userId}}&s={{surveyId}}">3</a>
        <a class="value" href="{{link}}/4?u={{userId}}&s={{surveyId}}">4</a>
        <a class="value" href="{{link}}/5?u={{userId}}&s={{surveyId}}">5</a>
        <a class="value" href="{{link}}/6?u={{userId}}&s={{surveyId}}">6</a>
        <a class="value" href="{{link}}/7?u={{userId}}&s={{surveyId}}">7</a>
        <a class="value" href="{{link}}/8?u={{userId}}&s={{surveyId}}">8</a>
        <a class="value" href="{{link}}/9?u={{userId}}&s={{surveyId}}">9</a>
        <a class="value" href="{{link}}/10?u={{userId}}&s={{surveyId}}">10</a>
    </div>

    <br/>
    <br/>
    <strong>Sua opinião é muito importante para nós.</strong>
    <h3>Equipe | <strong>Nome genérico</strong></h3>
</div>